# 🧾 원천세 신고 및 납부서 발행 자동화 (Withholding Tax Automation)

## 1. 프로젝트 개요 (Overview)
세무 회계 사무소의 가장 바쁜 기간인 매월 1~10일, **수천 개 거래처의 원천세 신고**를 처리하는 RPA 프로젝트입니다.
기존에 담당자가 일일이 수기로 입력하던 데이터를 **Power Automate Desktop(PAD)** 을 통해 100% 자동화하여 업무 부담을 줄이고 휴먼 에러를 제거했습니다.

### 🎯 핵심 성과 (Key Results)
- **시간 단축:** 거래처 100개 기준, 처리 시간 **10시간 → 2시간** (80% 단축)
- **정확도:** 데이터 검증 로직 도입으로 신고 오류 **0건** 달성
- **무인 구동:** 업무 시간 외(야간) 실행 가능한 **Unattended Bot** 구현

---

## 2. 프로세스 흐름 (Process Flow)
**[As-Is] 기존 업무 방식**
1. 담당자가 '위하고(WEHAGO)' 로그인 및 수임처 선택
2. 엑셀 급여대장을 보고 금액 수기 입력
3. 신고서 마감 후 홈택스,위택스 신고
4. 홈택스,위택스 납부서 출력 및 PDF 저장
5. 담당자에게 카카오톡 발송

**[To-Be] RPA 자동화 방식**
1. **Data Reading:** 엑셀 급여 데이터 일괄 로드 및 전처리
2. **Web Automation:** 위하고(WEHAGO) 접속 및 자동 로그인
3. **Data Entry:** 거래처별 신고서 자동 작성 (UI Automation)
4. **Validation:** (입력 금액 vs 엑셀 원본 금액) 교차 검증
5. **E-Filing:** 전자신고서 제작 후 각 홈페이지 신고서업로드 
6. **Reporting:** 납부서 PDF 저장 및 결과 리포트 생성

---

## 3. 핵심 기술 및 구현 (Key Features)

### 3.1 동적 UI 선택자 (Dynamic Selector) 처리
위하고(WEHAGO)와 같은 웹 ERP는 페이지 구조가 복잡하여 버튼이나 입력창을 찾지 못하는 경우가 많습니다. 이를 해결하기 위해 **가변적인 속성(ID, Class)** 대신 **계층 구조(Dom Path)** 와 **앵커(Anchor)** 기반의 UI 선택자를 최적화했습니다.

> **[예시]**
> * **문제:** 팝업창이 뜰 때마다 화면의 ID 값이 미세하게 바뀌어 봇이 멈추는 현상 발생
> * **해결:** UI 요소의 고유한 텍스트 속성(Text Attribute)을 기준으로 잡는 **Wildcard Selector** 기법 적용

### 3.2 데이터 무결성 검증 로직 (Data Validation)
단순 입력에 그치지 않고, 신고 버튼을 누르기 직전 **RPA가 입력한 값**과 **원본 엑셀 데이터**가 일치하는지 검증하는 안전장치를 마련했습니다.

```javascript
// [PAD 흐름 설명] 여러가지 검증단계 중 하나의 코드

IF (MainItem['지급총액'] = '' OR MainItem['지급총액'] = 0) = $'''True''' THEN
    **REGION 회계연도 설정
    SET Season TO MainItem['귀속년']
    WebAutomation.PressButton.PressButton BrowserInstance: SmartABrowser Control: appmask['Wehago(인사관리/퇴직소득관리)']['클릭: 연도 선택'] WaitForPageToLoadTimeout: 60
    WebAutomation.PressButton.PressButton BrowserInstance: SmartABrowser Control: appmask['Wehago(인사관리/퇴직소득관리)']['클릭: 연도'] WaitForPageToLoadTimeout: 60
    **ENDREGION
    CALL Subflow_Employee
    CALL Subflow_EmployeePayment
    Variables.ModifyDataTableItem DataTable: DTResult_Main ColumnNameOrIndex: $'''결과''' RowIndex: int_idxProcess + 1 Value: $'''작성 성공'''
END
```
